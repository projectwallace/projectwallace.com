# TODO explain why this is needed
AUTH_SECRET=test

# CDP WebSocket endpoint (incl. token) for Browserless, used by the /api/get-css
# route to render pages in a real browser instead of a plain fetch.
# e.g. wss://production-sfo.browserless.io?token=YOUR_TOKEN
BROWSERLESS_WS_ENDPOINT=